#include <stdio.h>
int main()
{
    int a,b;
    printf("Enter the 2 values to check\n");
    scanf("%d%d",&a,&b);
    if(a<b)
    {
    printf("b=%d is high than a=%d",b,a);
    }
    else if(a>b)
    {
    printf("a=%d is high than b=%d",a,b);
    }
    else 
    {
    printf("a=%d is equal to b=%d",a,b);
    }
    return 0;
}
